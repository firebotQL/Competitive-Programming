/**
 * Debugger: Explain what the following code does: ((n & (n - 1)) == 0)
 */
public class Main {
    public void main(String[] args) {
        // Assumption it is binary arithmetic
        // Lets take an example number 11 in binary
        // 1.   n - 1 should return 10 because 10 + 1 in binary equals 11
        // 2.   n & 11 should be equal to 10
        // 3.   10 == 0 equals false
    }
}
